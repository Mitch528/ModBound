<UserControl x:Class="ModBound.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:ModBound.ViewModels"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d"
             cal:Bind.AtDesignTime="True"
             d:DataContext="{d:DesignInstance IsDesignTimeCreatable=True, Type={x:Type viewModels:SettingsViewModel}}" Height="197" Width="299">
    <Grid>
        <CheckBox Content="Merge Mods" HorizontalAlignment="Left" Margin="95,82,0,0" VerticalAlignment="Top" Foreground="White" IsChecked="{Binding MergeMods}"/>
        <Button Content="Save" HorizontalAlignment="Left" Margin="194,153,0,0" VerticalAlignment="Top" Width="75" cal:Message.Attach="Save()" />
        <Button Content="Cancel" HorizontalAlignment="Left" Margin="20,153,0,0" VerticalAlignment="Top" Width="75" cal:Message.Attach="Cancel()"/>

    </Grid>
</UserControl>
